spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: USERNAME
    password: PASSWORD

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: public
